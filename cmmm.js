function CommonUnit(){
   this.fn_IS_APP = function(){
   var phoneOS ="";
   var agent = navigator.userAgent;
     if(agent){
        if(agent.match('iPhone/i) || agent.match(/iPod/i){}
     }
   }
}
